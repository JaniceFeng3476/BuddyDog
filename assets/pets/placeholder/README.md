# Placeholder Dog asset pack

This development-only asset pack is original project material generated by
`scripts/generate-placeholder-assets.ps1`. It does not contain downloaded or
third-party pet imagery.

The eight transparent 512 × 512 PNG frames use a fixed geometric character.
A sinusoidal phase changes only the small breathing offset and tail position so
the Idle animation can loop without duplicating its first frame.
